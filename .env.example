# 이 파일을 .env 로 복사한 뒤 실제 키를 넣는다. .env 는 커밋되지 않는다.
#
#   cp .env.example .env
#   vi .env
#   set -a; source .env; set +a

# 전자공시 OpenDART — https://opendart.fss.or.kr 에서 발급 (일 20,000건)
DART_API_KEY=

# 한국은행 ECOS — 비워 두면 공개 sample 키로 동작 (1회 10건 제한)
ECOS_API_KEY=
